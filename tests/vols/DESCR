Writes a file to the filesystem inside the container, then 
checks the content of that file from the host filesystem.

Checks:

  * Volume located in grua default
  * Volume in local folder (./local)
  * Volume in absolute location on host (/tmp)

